# Generate vocabulary

Make sure `JENA_HOME` is set:

```bash
export JENA_HOME=/path/to/jena/apache-jena-5.5.0
```

Uncomment the lines for the vocabulary files you wish to generate classes for in `build.sh`. Then run the script:
```bash
sh ./build.sh
```